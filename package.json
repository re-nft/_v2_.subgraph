{
  "name": "renft",
  "license": "MIT",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/nazariyv/ReNFT",
    "create-local": "graph create --node http://localhost:8020/nazariyv/ReNFT",
    "remove-local": "graph remove --node http://localhost:8020/nazariyv/ReNFT",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 nazariyv/ReNFT"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.19.0",
    "@graphprotocol/graph-ts": "^0.19.0"
  }
}
