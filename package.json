{
  "name": "@renft/subgraph",
  "license": "GPL-3.0-or-later",
  "version": "1.0.2",
  "author": "reNFT <hello@renft.io>",
  "scripts": {
    "codegen": "yarn graph codegen",
    "build": "yarn graph build",
    "deploy": "yarn graph deploy --studio renft-registry",
    "test": " yarn graph test "
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.30.1",
    "@graphprotocol/graph-ts": "^0.27.0",
    "matchstick-as": "^0.5.0"
  }
}
