{
  "name": "@renft/subgraphs-azrael",
  "license": "MIT",
  "version": "1.0.2",
  "author": "reNFT <hello@renft.io>",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --studio renft # todo: had to update deps below; implies that this command will no longer work, let's fix it"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.49.0",
    "@graphprotocol/graph-ts": "0.29.1"
  }
}
